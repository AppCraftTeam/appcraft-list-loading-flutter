name: appcraft_list_loading_flutter_example
description: Example usage of appcraft_list_loading_flutter — a ListView with pagination, pull-to-refresh, infinite scroll and search.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  appcraft_list_loading_flutter:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
