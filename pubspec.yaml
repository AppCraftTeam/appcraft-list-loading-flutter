name: appcraft_list_loading_flutter
description: Dispatcher для пагинированной загрузки списков с поддержкой offset/cursor, поиска (debounce) и стратегий отмены.
version: 0.0.1
repository: https://github.com/AppCraftTeam/appcraft-list-loading-flutter

topics:
  - pagination
  - loading
  - list
  - dispatcher

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  async: ^2.11.0
  flutter:
    sdk: flutter

dev_dependencies:
  fake_async: ^1.3.1
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
