name: appcraft_list_loading_flutter
description: Dispatcher for paginated list loading with offset/cursor support, debounced search and pluggable cancellation strategies.
version: 0.1.0
repository: https://github.com/AppCraftTeam/appcraft-list-loading-flutter

topics:
  - pagination
  - loading
  - list
  - dispatcher

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  async: ^2.11.0
  flutter:
    sdk: flutter

dev_dependencies:
  fake_async: ^1.3.1
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
